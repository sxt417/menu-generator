interface:
  display_name: "Menu Generator"
  short_description: "Turn dish photos into refined 3:4 fine-dining menu visuals"
  default_prompt: "Use $menu-generator to ask whether I want a Chinese or English version, then turn my uploaded dish photo into a refined 3:4 restaurant menu."
policy:
  allow_implicit_invocation: true
